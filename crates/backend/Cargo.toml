[package]
name = "backend"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
interface = { path = "../interface" }
parser = { path = "../parser" }
lexer = { path = "../lexer" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
